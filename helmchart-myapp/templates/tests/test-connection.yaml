apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmchart-myapp.fullname" . }}-test-connection"
  labels:
    {{- include "helmchart-myapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmchart-myapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
